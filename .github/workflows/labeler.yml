name: labeler
on:
  - pull_request_target
jobs:
  label:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - name: Clone repo
        uses: actions/checkout@v7
      - name: Add label
        uses: actions/labeler@v7
        with:
          sync-labels: true